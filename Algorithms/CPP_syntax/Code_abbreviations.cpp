#include <iostream>
#include <vector>
#include <cmath>

using namespace std;

/*
 * ============================================================
 *  СОКРАЩЕНИЕ КОДА: TYPEDEF И МАКРОСЫ
 * ============================================================
 *
 * 1. TYPEDEF (ПСЕВДОНИМЫ ТИПОВ)
 *    Ключевое слово typedef создаёт короткое имя для существующего типа.
 *    Это делает код компактнее и читаемее, особенно при частом использовании
 *    длинных названий (long long, vector<int>, pair<int,int> и т.д.).
 *
 * 2. МАКРОСЫ (#define)
 *    Макросы позволяют задать текстовую подстановку до компиляции.
 *    Они могут принимать параметры и помогают автоматизировать повторяющиеся
 *    конструкции (циклы, добавление элементов в контейнер и пр.).
 *    Важно помнить о скобках вокруг параметров и всего выражения, чтобы
 *    избежать неожиданного поведения из-за приоритета операторов.
 */

// ---------- TYPEDEF ----------
typedef long long ll;            // теперь ll эквивалентно long long
typedef vector<int> vi;          // vi — вектор целых чисел
typedef pair<int, int> pii;      // пара целых

// ---------- МАКРОСЫ ----------
// Макрос для добавления элемента в конец вектора
#define PB push_back

// Макрос для цикла for по возрастанию: REP(i, a, b) -> for(int i=a; i<=b; ++i)
#define REP(i, a, b) for (int i = (a); i <= (b); ++i)

// Макрос для цикла for по убыванию
#define REPD(i, a, b) for (int i = (a); i >= (b); --i)

int main() {
    // ---------------------------------------------------------
    // Пример использования typedef
    // ---------------------------------------------------------
    ll big_number = 123456789012345LL;   // long long
    vi numbers;                          // vector<int>
    
    REP(i, 1, 5) {                      // заполняем вектор
        numbers.PB(i * 10);             // push_back через макрос
    }

    // Вывод результата
    cout << "big_number = " << big_number << '\n';
    cout << "numbers = ";
    REP(i, 0, numbers.size() - 1) {     // обратите внимание: индексы с 0
        cout << numbers[i] << ' ';
    }
    cout << '\n';

    // ---------------------------------------------------------
    // Важное замечание о макросах
    // ---------------------------------------------------------
    // Макросы — мощный, но опасный инструмент.
    // Всегда заключайте параметры и всё выражение в скобки.
    // Пример неправильного макроса:
    //    #define SQUARE(x) x*x
    //    SQUARE(1+2) -> 1+2*1+2 = 5 (а не 9).
    // Правильно:
    //    #define SQUARE(x) ((x)*(x))

    // В современном C++ часто вместо макросов используют constexpr,
    // inline-функции или лямбды, но в олимпиадном программировании
    // короткие макросы допустимы для ускорения написания кода.

    return 0;
}
